# img

Empty for now. Reserved for per-breed images to render inside each timeline
marker's circle (the original plan in `../../../tasks.md`). No image source
with clear usage rights exists yet — see the "Known issues" section of
`../../README.md` for details. `index.html` does not reference anything in
this folder yet.
